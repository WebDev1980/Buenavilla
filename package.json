{
  "name": "vecinos-buenavilla",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "set \"GITHUB_PAGES=true\" && vite build && git add dist -f && git commit -m \"Deploy a GitHub Pages\" && git subtree push --prefix dist origin gh-pages",
    "update": "git diff --quiet && echo 'Sin cambios para commitear' || (git add . && git commit -m \"Actualización de código\" && git push origin main && npm run deploy)"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.0"
  }
}
